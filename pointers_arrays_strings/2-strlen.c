#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - check the code
 * @s: variable
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int length = strlen(str);

	printf("%d", length);

	return (0);
}
